# git command

#add file  - git add .
#commit     - git commit -m "add files or any query"
#push       - git push origin main
#stats       - git status
#gh cmd

#clone       - gh repo clone poova1616/infinity-trader

python package install
#Make sure you have yfinance installed:

pip install yfinance
pip install plotly

python.exe -m pip install --upgrade pip
